import 'package:flutter/material.dart';

class MenuPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "هذه صفحة القائمة (Menu)",
        style: TextStyle(fontSize: 22),
      ),
    );
  }
}